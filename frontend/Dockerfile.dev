# 개발용 Dockerfile
# 핫 리로딩 및 개발 도구 포함

FROM node:18-alpine

WORKDIR /app

# package.json과 package-lock.json 복사
COPY package*.json ./

# 의존성 설치 (개발용 포함)
RUN npm ci

# 소스 코드 복사
COPY . .

# 포트 노출
EXPOSE 3000

# 개발 서버 실행 (핫 리로딩)
CMD ["npm", "start"]
